project: "GoAcademy"
last_known_stage: "STAGE 18.3"     # ПРОЕКТ ЗАВЕРШЁН — 57/57 этапов (главы 0–18). Всё в рабочем дереве, не закоммичено.
critical_info:
  - "ПРОЕКТ ЗАВЕРШЁН: 57/57 этапов (100%). Бэкенд (Go/chi/pgx/sqlc/JWT) + весь фронтенд (React/TS/Vite/i18n RU·EN·UZ·JA) + code-runner/песочница + онлайн-судья + security + CI + полный docker-compose. ВСЁ в рабочем дереве — НИЧЕГО не закоммичено (по требованию: коммит только по явной просьбе)."
  - "18.3 — деплой: полный deploy/docker-compose.yml (postgres→migrate→backend→frontend); backend/Dockerfile (multi-stage, non-root, встроенные миграции); frontend/Dockerfile (vite→nginx) + nginx.conf (proxy /api+/static на backend → один origin, без CORS); фронт на относительном /api/v1 + Vite/nginx proxy (исправлен CORS-gap). README переписан (docker compose up -d --build → :3000). ЖИВАЯ ПРОВЕРКА: все сервисы healthy, /api/v1/videos через :3000 проксируется (total=2), /me→401. ВКЛ. ПЕСОЧНИЦУ/СУДЬЮ: SANDBOX_ENABLED=true + Docker+Go на хосте backend."
  - "18.2 — CI/линтеры: .github/workflows/ci.yml (backend fmt/vet/build/test; backend-integration с Postgres; frontend lint/build). Покрытие 67–94%."
  - "Запуск с нуля: docker compose -f deploy/docker-compose.yml up -d --build → http://localhost:3000. Сменить JWT_SECRET и POSTGRES_PASSWORD для прода."
  - "18.1 — security pass: middleware securityHeaders (nosniff/DENY/no-referrer/COOP/Permissions-Policy/restrictive CSP) глобально; УБРАН RealIP (анти-спуфинг rate-limit — ключ на TCP-peer); rate-лимитер TTL-эвикция (закрыт open-question); аудит логов (нет Authorization/cookie/body/query; токены не логируются); CSRF = SameSite=Lax cookie + Bearer-заголовок на state-changing. RULES.md §8 10/10 [x]. Тесты security-заголовков + rate-limiter eviction PASS. open_questions: rate-map RESOLVED; user-enum + XP-dup пересмотрены→приняты для MVP (не блокируют деплой)."
  - "17.2 — интеграция судьи: problem_handler.submit → judge.Submit при Deps.Judge!=nil (вердикт+авто-solved+reveal reference при OK); ErrNoTestCases→fallback на ручной progress.SubmitProblem. dto +Verdict *judge.Result. main: judgeService при cfg.SandboxEnabled (судья и sandbox делят runner). Фронт ProblemDetail: VerdictPanel (overall+passed/total+per-case sample/hidden+compile_error). Live e2e: correct→OK 4/4+reveal, wrong→WA (hidden НЕ утекают), CE→compile_error, fallback (без кейсов)→manual. ВАЖНО: судья/sandbox требуют SANDBOX_ENABLED=true (Docker+Go); иначе ручной режим."
  - "17.1 — судья-движок: runner +RunBatch (компиляция×1). internal/judge (Verdict OK/WA/TLE/RE/CE, normalize/classify, Judge.Run, Service.Submit persist+activity). store/judge_queries.go (hand-written: ListProblemTestCases, CreateJudgedSubmission). Тесты unit+интеграция+persistence PASS."
  - "16.2 — песочница: config SANDBOX_ENABLED/...; POST /sandbox/run под RequireAuth+RateLimit (→runner.Run); Sandbox UI + Markdown-виджет. Live e2e ОК."
  - "16.1 — code-runner (D-027): backend/internal/runner — Go компилируется на ХОСТЕ (CGO off, GOPROXY off → stdlib-only), статический бинарник в одноразовом Docker (network none, cap-drop ALL, no-new-privileges, non-root, mem/cpu/pids, tmpfs, wall-timeout, output cap; docker create -i→cp→start; OOM через inspect; образ busybox). Runner.Run→Result. 9 интеграционных тестов PASS. Docker 29.2.1."
  - "15.8 — админ-панель UI: AdminRoute-гейт (role admin, иначе redirect; бэкенд RequireRole независимо); nav.admin (adminOnly, виден только админу); AdminHome/AdminVideos+VideoForm/AdminArticles+ArticleForm (CRUD flat-контента, Markdown-textarea)/AdminUsers (поиск+пагинация+тоггл роли/блокировки, клиентская самозащита). Live e2e: gate 403, video/article CRUD, user promote+block, self-demote 403. Квизы/задачи/прочее — тот же паттерн (задел)."
  - "15.7 — фронт social/профиля: lib/api +FormData (api.upload аватара); useLeaderboard/useNotes(CRUD)/useBookmarks(CRUD)/useUpdateProfile/useUploadAvatar; lib/contentPath. Leaderboard, MyNotes, MyBookmarks, Profile (PATCH /me + аватар + setUser). ContentActions (BookmarkButton+NoteComposer) на Article/VideoDetail. nav +notes/+bookmarks."
  - "15.6 — фронт шпаргалок/глоссария/проектов (бэкенд готов с CH9, без изменений): useCheatsheets/useCheatsheet/useGlossary/useProjects/useProject/useProjectProgress/useToggleProjectStep; CheatsheetList+Detail (поиск, Markdown), Glossary (поиск), ProjectList+Detail (чек-лист toggle + прогресс). Роуты /cheatsheets(/:id), /glossary, /projects(/:id)."
  - "15.5 — фронт треков + БЭКЕНД D-026: контент адресуется по id-ИЛИ-slug (GET /articles/{ref},/problems/{ref} и под-роуты принимают UUID или slug; новые hand-written store-методы GetArticleByID/GetProblemByID в internal/store/content_byid.go БЕЗ перегенерации sqlc; progress.resolveArticle/resolveProblem) — нужно для навигации из программы трека (track_items по content_id UUID). TrackList + TrackDetail (полоса прогресса, программа по порядку с иконками типов + чекмарки + ссылки по content_id)."
  - "15.4 — фронт задач (бэкенд готов с CH7, без изменений): lib/queries useProblems/useProblem/useProblemSolution(403→null=ещё не решено)/useSubmitProblem; ProblemList + ProblemDetail (условие через ленивый Markdown; sample_io примеры; редактор textarea + select языка; чекбокс «отметить решённой» + Submit; reveal эталона после solved; нота о будущем судье CH17)."
  - "15.3 — фронт квизов (бэкенд готов с CH6, без изменений): lib/queries useQuizzes/useQuiz/useSubmitQuiz (POST /quizzes/{id}/attempts с {answers:{questionId:optionId[]}}); QuizList + QuizDetail (radio single / checkbox multiple; «Отправить» только когда отвечены все; баннер результата score/passed + per-question разбор через correct_option_ids; «Пройти заново»)."
  - "15.2 добавил бэкенд GET /articles/{slug}/read (статус прочтения). Фронт: components/Markdown (react-markdown 10 + remark-gfm + rehype-highlight только go/bash/json/yaml/sql/dockerfile; САНИТИЗАЦИЯ = react-markdown не рендерит сырой HTML + дефолтный URL-transform отбрасывает javascript:; лениво грузится отдельным чанком, main 391kB/Markdown 323kB); ArticleList/ArticleDetail (кнопка «прочитано» идемпотентна, индикатор через GET read); кнопка «Открыть в песочнице» = заглушка-навигация /sandbox до CH16. Решение D-025."
  - "15.1 добавил бэкенд GET /videos/{id}/progress (для восстановления позиции). Фронт: YouTubePlayer (ручной YT IFrame API), VideoList/VideoDetail, прогресс-трекинг, авто/ручная отметка."
  - "Node.js v24.18.0 установлен (winget). ВАЖНО: tool-шеллы НЕ видят node в PATH — команды начинать с: $env:Path='C:\\Program Files\\nodejs;'+$env:Path."
  - "Фронтенд (frontend/): Vite8+React19+TS, Router7, Tailwind v4, @→src, тема (D-023). Auth (D-024): TanStack Query, lib/api (in-memory access+refresh-cookie, авто-refresh, ApiError), AuthProvider/auth-context, ProtectedRoute. i18n: i18next ru/en/uz/ja, LanguageSwitcher, profile-locale. Дашборд (14.4): query-хуки /me/*, XP/уровень/streak, бейджи, ActivityHeatmap (GitHub). build/lint/typecheck зелёные (0 warnings). Браузерный e2e не автоматизируется (нет драйвера)."
  - "ВСЕ ГЛАВЫ 0–18 ЗАВЕРШЕНЫ (57/57). CH18: 18.1 security ✅, 18.2 CI ✅, 18.3 docker-compose+докум ✅."
  - "Backend: конфиг/HTTP/slog/graceful; Postgres+golang-migrate (миграции до 0023); sqlc→store; единый формат ошибок; полный API."
  - "Auth: register/verify(stub)/login/refresh-ротация+reuse-detection/logout/reset/роли(student|admin)/rate-limit. RequireRole(admin). Профиль+аватар."
  - "Контент (чтение+прогресс): видео, статьи, квизы, задачи (MVP), треки, шпаргалки, глоссарий, мини-проекты. activity_log (0017) + дашборд (/me/progress, /me/activity heatmap)."
  - "Геймификация целиком (0018–0020): internal/gamification, transactional Recorder под FOR UPDATE — XP, уровень, streak, бейджи, daily. Решения D-017..D-020."
  - "Соц. фичи (internal/social): лидерборд (D-021); заметки (0021); закладки (0022); сертификаты (0023 — выдача при 100% трека, публичная верификация GET /certificates/{code}, D-022)."
  - "Admin (internal/admin): CRUD под RequireRole('admin') ВСЕХ типов контента (13.1–13.2) + управление пользователями (13.3: список/поиск/пагинация, смена роли, блокировка, самозащита). Миграций в CH13 нет."
  - "Стек: Go(chi/pgx/sqlc/golang-migrate/JWT/argon2id/slog) + PostgreSQL 17; фронт (CH14, заблокирован) React/TS/Vite/TanStack/Tailwind/shadcn/i18next; code-runner с CH16."
  - "Окружение: контейнер goacademy-postgres (healthy, :5432), миграции применены до 0023. Node.js/npm НЕ установлены."
  - "Всего этапов: 57 (CHAPTER 0..18). Решения D-001..D-022 в ARCHITECTURE.md и STATUS.yaml.decisions."
open_blockers:
  - "Нет блокеров. ПРОЕКТ ЗАВЕРШЁН (57/57). На security pass 18.1 разобраны открытые вопросы: rate-limiter map — закрыт (TTL-эвикция); user enumeration и редкий конкурентный дубль XP — пересмотрены и приняты для MVP (не блокируют деплой). Опциональные пост-проектные расширения — в STATUS.next_actions. Всё в рабочем дереве, не закоммичено."
last_update: "2026-06-26"
